import { Metadata } from "next";

export const metadata: Metadata = {
  title: 'Work',
}

const Work = (): React.ReactNode => {
    return (
        <div>
            <h1>my work</h1>
        </div>
    )
}

export default Work;
